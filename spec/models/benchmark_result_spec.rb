# spec/models/benchmark_result.rb
require 'spec_helper'

describe "BenchmarkResults" do
    it "belongs to benchmark_report" do
        #expect(benchmark_result).to belong_to(:benchmark_report)
    end
  it "has a valid name"
  it "has a valid score"
  it "has a valid elapsed time"
end